PORT=3000

# Relative to process.cwd() or absolute path (TypeORM sqljs + sql.js package)
SQLITE_DATABASE=data/app.sqlite

# Set true in dev to auto-create/update tables from entities
DB_SYNC=true
DB_LOGGING=false
